
import options from '../modules/actions'

export const onlinePad = options.online.clickPadOnline;
export const offlinePad = options.offline.clickPad;
export const onlineGame = options.online.startGameOnline;
export const offlineGame = options.offline.startGame;
export const leaveGame = options.online.leaveGame;
export const reset = options.reset;
export const waitPlayers = options.waitPlayers;
